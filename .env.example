# ClinicFlow Environment Variables
# Copy this file to .env and update the values

# Database Connection
# Format: postgresql://username:password@host:port/database_name
DATABASE_URL=postgresql://neondb_owner:npg_6IiAcqasMD7k@ep-holy-firefly-a1e7159d-pooler.ap-southeast-1.aws.neon.tech/clinicq?sslmode=require&channel_binding=require

# Alternative: Individual database settings (if not using DATABASE_URL)
# PGHOST=localhost
# PGPORT=5432
# PGUSER=clinicflow
# PGPASSWORD=your_password
# PGDATABASE=clinicflow_db

# Server Configuration (optional - defaults shown)
# PORT=3001
# NODE_ENV=development

# IMPORTANT NOTES:
# 1. DATABASE_URL is REQUIRED - the backend will crash without it
# 2. PostgreSQL must be running before starting the application
# 3. If you change PORT from 3001, you must also update the proxy
#    settings in vite.config.ts to match the new port
